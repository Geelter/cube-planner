{
  // Tailwind v4 at-rules (@theme, @custom-variant, @apply) are unknown to the
  // built-in CSS validator; the Tailwind IntelliSense extension provides real
  // support (see extensions.json).
  "css.lint.unknownAtRules": "ignore"
}
